PORT=4000
FRONTEND_URL=http://localhost:5173
DB_LOGGING=false

# Option 1: Direct connect string
# DATABASE_URL=postgres://user:password@localhost:5432/investo

# Option 2: Host / credential breakdown (mirrors Investo)
DB_HOST=127.0.0.1
DB_PORT=5432
DB_NAME=investo
DB_USER=postgres
DB_PASSWORD=
DB_SSL=false

